#pragma once

namespace Controls {
	enum Controls {
		Up,
		Down,
		Forward,
		Back,
		Left,
		Right,
		Jump,
		Sprint,
		Exit,
		LeftClick,
		RightClick,
		ToggleDebug,
		ControlCount
	};

	enum InputAxes {
		AxisCount
	};
}