#pragma once

namespace Controls {
	enum Controls {
		Up,
		Down,
		Forward,
		Back,
		Left,
		Right,
		Jump,
		Sprint,
		Exit,
		ControlCount
	};

	enum InputAxes {
		Horizontal,
		Vertical,
		AxisCount
	};
}